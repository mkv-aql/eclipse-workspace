package de.tuhh.diss.SS20;

public class LedStrip extends Lamp {
    private LightingScene[] scenes;

    @Override
    public boolean setScene(LightingScene scene) {
        // Code here to add scene to the scenes array and return true if successful
        // We need to check if the scene is supported before adding it
        return false; // Replace with actual implementation
    }
}
